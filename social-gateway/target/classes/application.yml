spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
      - id: social-user-service
        uri: lb://social-user-service
        predicates:
        - Path=/user/**
      - id: social-profile-service
        uri: lb://social-profile-service
        predicates:
        - Path=/profile/**   
info:
  app:
    name: social-gateway
    version: 0.1-SNAPSHOT
    decription: 0.1-SNAPSHOT